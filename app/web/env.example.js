// Copy this file to `web/env.js` and fill in your real Google Maps
// JavaScript API key. `web/env.js` is git-ignored — never commit real keys.
window.APP_ENV = {
  GOOGLE_MAPS_API_KEY: "your-google-maps-js-api-key"
};
