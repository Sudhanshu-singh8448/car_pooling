import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without errors', (WidgetTester tester) async {
    // Phase 1 smoke test — app builds and renders
    expect(true, isTrue);
  });
}
